org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# ---------- Keep these in lockstep with SimDistance / ClaimViz — same MC/Yarn/Fabric target ----------
# Find Yarn builds at: https://fabricmc.net/develop/
#
# UPGRADING TO A NEW MINECRAFT VERSION (e.g. 26.3):
#   Everything version-specific lives in THIS file. To retarget, bump the five
#   values below (and the two soft-dep versions) to the matching builds from the
#   pages linked here — no source changes should be required for a normal bump.
minecraft_version=26.1.2
loader_version=0.19.3
fabric_version=0.155.2+26.1.2
# ----------------------------------------------------------------------------------------------------

# Soft dependency versions — must match what's available for the MC version above
# ModMenu releases:     https://modrinth.com/mod/modmenu/versions
# Cloth Config releases: https://modrinth.com/mod/cloth-config/versions
modmenu_version=18.0.0
cloth_config_version=26.1.154+fabric
basic_math_version=0.6.1

trarncore_version=1.3.0

mod_version=0.3.0
maven_group=net.easyportallinker
archives_base_name=easyportallinker
